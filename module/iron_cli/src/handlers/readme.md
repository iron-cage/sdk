# Directory: handlers

## Responsibility Table

| File | Responsibility |
|------|----------------|
| auth_handlers.rs | Handles authentication command validation logic |
| error.rs | Defines handler layer error types |
| health_handlers.rs | Handles health check command validation |
| limits_handlers.rs | Handles limit management command validation |
| mod.rs | Exports handler layer modules and architecture |
| token_handlers.rs | Handles token CRUD command validation |
| traces_handlers.rs | Handles trace query command validation |
| usage_handlers.rs | Handles usage tracking command validation |
| user_handlers.rs | Handles user management command validation |
| validation.rs | Provides common validation helper functions |

## Validation

One-Second Test: Scan the Responsibility column - if any two entries sound similar, they overlap and must be reconsolidated.
