# src/composables

Vue composables that encapsulate reusable stateful logic.

| File | Purpose |
|------|---------|
| `useApi.ts` | Typed API client wrapping all backend HTTP calls |
| `useConfirm.ts` | Reusable confirm-dialog state shared across views |
