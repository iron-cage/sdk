# src/lib

Shared utility modules used across the application.

| File | Purpose |
|------|---------|
| `formatters.ts` | Pure formatting functions for dates, currency, and numbers |
| `providers.ts` | Provider metadata and helper utilities |
