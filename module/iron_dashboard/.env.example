# Iron Control API base URL. Defaults to http://localhost:3001.
VITE_API_URL=http://localhost:3001

# Iron Cage gateway base URL shown to members on their dashboard.
# If unset, the dashboard falls back to window.location.origin.
VITE_GATEWAY_URL=http://localhost:8080
